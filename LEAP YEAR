def ISLP(y):
  if((y % 400 == 0) or
     (y % 100 != 0) and
     (y % 4 == 0)):
         print("It is a leap year")
  else:
      print("It is not a leap year")
if __name__=='__main__':
   year = 2020;
  print(ISLP(year));
